# Data

The original claim-level healthcare dataset is not included in this public repository.

Schema:
```text
claim_id
patient_id
provider_id
procedure_code
claim_date
payment_date
expected_amount
paid_amount
claim_status
```

Use a synthetic or appropriately de-identified dataset if reproducing the project.
